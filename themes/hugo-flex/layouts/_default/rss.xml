{{- $base_xml := resources.Get "xml/base.tpl.xml" | resources.ExecuteAsTemplate "xml/base.xml" . | minify }}
{{- $pages := where site.RegularPages "Type" "in" site.Params.mainSections }}
{{- $featured := where $pages "Params.type" "une" }}
{{- $limit := site.Config.Services.RSS.Limit }}
{{- $total := len $featured }}
{{- if gt $limit 0 }}
  {{- if gt $total $limit }}
    {{- $featured = first $limit $featured }}
  {{- end }}
  {{- $remaining := sub $limit $total }}
  {{- $pages = first $remaining $pages }}
{{- end }}
{{- safeHTML `<?xml version="1.0" encoding="utf-8" ?>` }}
{{ printf `<?xml-stylesheet type="text/xsl" href=%q ?>` $base_xml.Permalink | safeHTML }}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ if eq .Title site.Title }}{{ site.Title }}{{ else }}{{ with .Title }}{{ . }} on {{ end }}{{ site.Title }}{{ end }}</title>
    <link>{{ .Permalink }}</link>
    <description>Recent content {{ if ne .Title site.Title }}{{ with .Title }}in {{ . }} {{ end }}{{ end }}on {{ site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>{{ with site.LanguageCode }}
    <language>{{ . }}</language>{{ end }}{{ with site.Params.author.email }}
    <managingEditor>{{ . }}{{ with site.Params.author.name }} ({{ . }}){{ end }}</managingEditor>{{ end }}{{ with site.Params.author.email }}
    <webMaster>{{ . }}{{ with site.Params.author.name }} ({{ . }}){{ end }}</webMaster>{{ end }}{{ with site.Copyright }}
    <copyright>{{ . }}</copyright>{{ end }}
    {{- range $featured }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      {{ with .PublishDate | default nil }}
      <pubDate>{{ time.Format "Mon, 02 Jan 2006 15:04:05 -0700" . | safeHTML }}</pubDate>
      {{ end }}
      {{ with .Params.author }}
      <author>{{ . }}</author>
      {{ end }}
      <guid>{{ .Permalink }}</guid>
      <description>{{ html .Content }}</description>
    </item>
    {{- end }}
    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      {{ with .PublishDate | default nil }}
      <pubDate>{{ time.Format "Mon, 02 Jan 2006 15:04:05 -0700" . | safeHTML }}</pubDate>
      {{ end }}
      {{ with .Params.author }}
      <author>{{ . }}</author>
      {{ end }}
      <guid>{{ .Permalink }}</guid>
      <description>{{ html .Content }}</description>
    </item>
    {{- end }}
  </channel>
</rss>