{{- $pages := where site.RegularPages "Type" "in" (slice "articles" "une" "telex") }}
{{- $pages := where $pages "PublishDate" "ge" (time "2024-01-01") }}
{{- $pages := sort $pages "PublishDate" "desc" }}
{{- $limit := site.Config.Services.RSS.Limit }}
{{- if ge $limit 1 }}
  {{- $pages = first $limit $pages }}
{{- end }}
{{- $enableSummary := site.Params.rss.enableSummary | default true }}
<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ site.Title }}</title>
    <link>{{ site.BaseURL }}</link>
    <description>Recent content on {{ site.Title }}</description>
    <generator>Hugo -- gohugo.io</generator>
    {{- with site.LanguageCode }}
    <language>{{.}}</language>
    {{- end }}
    {{- with site.Author.email }}
    <managingEditor>{{.}}{{ with site.Author.name }} ({{.}}){{end}}</managingEditor>
    {{- end }}
    {{- with site.Author.email }}
    <webMaster>{{.}}{{ with site.Author.name }} ({{.}}){{end}}</webMaster>
    {{- end }}
    {{- if not .Date.IsZero }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    {{- with site.Params.rss.atom }}
    <atom:link href="{{ . }}" rel="self" type="application/rss+xml" />
    {{- end }}
    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ .Permalink }}</link>
      <pubDate>{{ .PublishDate.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with .Site.Author.email }}
      <author>{{.}}{{ with $.Site.Author.name }} ({{.}}){{end}}</author>
      {{- end }}
      <guid>{{ .Permalink }}</guid>
      {{- if $enableSummary }}
      <description>{{ .Summary | html }}</description>
      {{- else }}
      <description>{{ .Content | html }}</description>
      {{- end }}
    </item>
    {{- end }}
  </channel>
</rss>